<div class="modal-content">
    <%= form_for([@event, @attendance || @event.attendances.build], remote: true) do |form| %>
        <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">Join Event</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
        <div class="modal-body">
            
            <div class="row">
                <%= form.label :accompanied_number, "Number of people", class: "control-label col-sm-4" %>
                <div class="col-sm-8">
                    <%= form.number_field :accompanied_number, class: 'form-control attend-number-box', autofocus: true, :id => 'attend-number-box', min: 2, max: 10 %>
                </div>
            </div>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <%= form.submit form.object.new_record? ? "Join Event" : "Update Joining Event", id: "confirm-join", class: "btn btn-primary" %>
        </div>
    <% end %>
</div>
